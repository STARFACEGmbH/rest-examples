const SfConfig = function () {
    return {
        // Enter the IP or Domain of the STARFACE PBX to connect with:
        host: '0.0.0.0 | example.com',
        // Enter the HTTP(S) port number of the STARFACE PBX to connect with:
        port: '80 | 443 | xxx',
        // Enter the web protocol scheme of the STARFACE PBX to connect with:
        scheme: 'https | http'
    };
};
